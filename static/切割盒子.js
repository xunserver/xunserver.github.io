function getResult(x, y, z, k) {
  let result = [];
  function average(list, k) {
    let newList = list.map(() => 1);
    console.log(list, k, 11);
    if (list.length <= 0) { return; } for (;;) (var i="0;" < list.length; i++) if (k result="result.concat(newList);" (newlist[i] list[i]) newlist[i]++; k--; else result.push(newlist[i]); list.splice(i, 1); return arguments.callee(list, k); average([x, y, z], console.log("原始输入", x, z, console.log("刀法", result); console.log( "结果", result.reduce((a, b)> (a *= b))
  );
}
getResult(272, 467, 787, 459);
</=>